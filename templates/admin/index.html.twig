{% extends 'adminbase.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <div class="bg-white rounded-lg shadow p-6">
        <h1 class="text-2xl font-bold mb-4">Admin Dashboard</h1>
        <button class="bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded">
            <a href="{{ path('importProductsApi') }}">Importer Produits</a>
        </button>
        <button class="bg-amber-500 hover:bg-amber-300-600 text-white font-semibold py-2 px-4 rounded">
            <a href="{{ path('importUsersApi') }}">Importer Utilisateurs</a>
        </button>
        <button class="bg-green-500 hover:bg-green-300-600 text-white font-semibold py-2 px-4 rounded">
            <a href="{{ path('importCategoriesApi') }}">Importer Categories</a>
        </button>
    </div>
{% endblock %}
